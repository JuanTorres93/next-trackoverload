import PageWrapper from '../../_ui/PageWrapper';

export const metadata = {
  title: 'Comidas',
  description: 'Planificación de comidas',
};

export default function Dashboard() {
  return <PageWrapper>Planificación comidas</PageWrapper>;
}
